import { useNavigate, useParams } from "react-router-dom";

export default function ProdutoDetalhe(){
    const {id} = useParams();
    const navigate = useNavigate();
   // const { data: produto, loading } = 
}